# Set real values in Sites runtime environment settings, never in client code.
SUPPORT_ENABLED=false
OPENAI_API_KEY=
OPENAI_MODEL=
# Stable per-Site identity from the trusted dispatcher, not the account email.
SUPPORT_ALLOWED_USER_IDS=
