# Vibe — نسخة كود الموقع الحالية

هذه نسخة من المصدر المنشور بعد إعادة شاشة رمز التحقق، وتشمل الواجهات والخادم والصور وملفات SQL والاختبارات ونسخة Worker المبنية.

الرابط: https://vibe-social-nights.bb0949.chatgpt.site

## التشغيل والبناء
من مجلد Vibe شغّل باستخدام Node.js حديث:

```sh
npm ci
npm run build
npm test
node tests/auth-flow.mjs
```

ينتج البناء dist/server/index.js وهو خادم Cloudflare Workers بأصول واجهة مدمجة. فتح public/index.html وحده لا يشغّل الخادم أو المصادقة.

## محتويات النسخة
- public/: الواجهات والتصاميم والصور ومنطق المصادقة.
- server/: تكامل Supabase والخادم والسينما.
- db/ وdrizzle/: ملفات المخطط والهجرات الموجودة في المستودع.
- tests/: الاختبارات، بما فيها اختبار مسار الكود بمحاكاة الشبكة.
- dist/: ملفات البناء.
- docs/ وappstore/: الوثائق الموجودة، وبعضها يصف مراحل أقدم؛ README.md الأصلي وثيقة تاريخية ولا يصف كل التعديلات الحديثة.

## حدود النسخة
هذه نسخة ملفات الموقع، وليست نسخة احتياطية من بيانات Supabase الحية أو حسابات المستخدمين أو الملفات المرفوعة أو إعدادات SMTP وقوالب البريد المحفوظة في لوحة التحكم. ملفات SQL هي الملفات الموجودة بالمستودع وليست تفريغًا حديثًا لقاعدة البيانات. لا تحتوي النسخة على المشروع الأصلي لـ Xcode.

لا تتضمن كلمات مرور أو مفاتيح خدمة سرية أو مجلد Git أو node_modules. تحتوي على مفتاح Supabase العام المستخدم بالموقع؛ ليس مفتاح خدمة سريًا. الاتصال مرتبط بالمشروع الحالي. لنقل الموقع إلى مشروع أو عنوان مختلف، حدّث عناوين Supabase والمفتاح العام في server/cloud.mjs وpublic/cloud-ui.js، والعنوان الموثوق وسياسة الاتصال في server/worker.mjs وpublic/index.html وpublic/_headers، واضبط إعدادات المصادقة بالخدمة. لا تعِد تشغيل SQL عشوائيًا على قاعدة البيانات الحالية.

المصادقة: كلمة المرور متاحة؛ التسجيل والحساب غير المؤكد يعرضان شاشة إدخال رمز البريد، مع إعادة الإرسال وخيار الدخول برمز. اختبار وصول البريد الحقيقي ليس جزءًا من الاختبارات المرفقة.
